const generateSubjectHeadings = (title, author) => {
    // Dummy logic for generating subject headings
    return [`Heading for ${title}`, `Heading for ${author}`];
};

const generateKeywords = (title, author) => {
    // Dummy logic for generating keywords
    return [`Keyword1 for ${title}`, `Keyword2 for ${author}`];
};

module.exports = { generateSubjectHeadings, generateKeywords };
